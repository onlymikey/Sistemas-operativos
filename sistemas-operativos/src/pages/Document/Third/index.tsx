import Document from "../../../components/Document";

export default function DocumentThird(): JSX.Element {
    return (
        <Document title="Administracion de procesos" urlTo="./../tercero.pdf">
            Administacion de procesos nos muestra diferentes tipos de ordernar y asegurar un uso justo de la CPU entre todos los procesos del sistema operativo.
        </Document>
    )
}